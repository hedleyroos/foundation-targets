from setuptools import setup, find_packages

setup(
    name='foundationtargets',
    description='',
    version='',
    author='',
    license='BSD',
    url='',
    packages = find_packages(),
    dependency_links = [
    ],
    install_requires = [
        'django',
        'django-geckoboard',
    ],
)
